# generated: 2026-05-26
# last_updated: 2026-05-30 (Story 4.3a code-review complete — Status: done; 2 decision-needed resolved (preview banner / accept token-default), 7 patches applied (preview admonition + 3 troubleshooting rewrites + SEMVERTAG_PROVIDER env + --builtin-schema swap + heading + Required-setup callout + PAT example), 10 deferred to _bmad/deferred-work.md, 5 dismissed; gates re-validated green)
# project: semvertag
# project_key: NOKEY
# tracking_system: file-system
# story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-26
last_updated: 2026-05-30
project: semvertag
last_updated_note: "Story 4.3a dev complete — Status: review. NEW action.yml at repo root (44 LOC YAML): composite action wrapping `uvx semvertag`; `name: 'semvertag'`, `description:` ≤125 chars, `author: '<org>'` placeholder, `branding: { icon: tag, color: blue }`, inputs `strategy` (default `'branch-prefix'`) + `token` (default `${{ github.token }}`), `runs.using: 'composite'` with `astral-sh/setup-uv@v3` (no version pin per Constraint 8) + `Run semvertag` step (`shell: bash`, env `GITHUB_TOKEN` + `SEMVERTAG_STRATEGY`, `run: uvx semvertag`). NEW docs/providers/github.md (117 LOC markdown): six sections — Quick Start (15-line minimal workflow byte-equal to AC6 literal, incl. `<org>` placeholder), Inputs (table mirroring action.yml), Required permissions (`contents: write` + Journey 3 error wording), Token scope (PR vs main + protected-branch footgun), Branch-prefix vs conventional-commits routing, Troubleshooting (4 failure modes). mkdocs.yml: +2 LOC `Providers → GitHub Actions: providers/github.md` nav between Quick Start and Contributing; theme/palette/extras byte-stable. ci.yml lint job: +9 LOC single step `Validate action.yml against GitHub Actions schema` between `just install lint-ci` and `uv build`, runs `uvx --from check-jsonschema check-jsonschema --schemafile https://json.schemastore.org/github-action.json action.yml`; pytest matrix + pip-audit untouched. Gates green: action.yml schema-validate (`ok -- validation done`), action.yml + ci.yml yaml.safe_load (`YAML OK`), `just lint-ci` (eof-fixer + ruff format + ruff check + ty check), 425 tests pass in 1.18s, branch/cc/doctor 26/44/56 100% branch, `uv run ty check`, `uv build` (sdist + wheel), `mkdocs build --strict` 0.22s. AC11 + AC13 byte-exact preserved (`git diff HEAD --` against semvertag/ tests/ pyproject.toml Justfile README.md LICENSE docs/index.md docs/contributing/ publish.yml dependency-update.yml .gitlab/ all empty). 2 deviations from spec literal documented in Change Log: (a) Task 2.7 dropped dead links to docs/strategies/*.md because mkdocs `--strict` rejects them (Story 4.4 owns those pages; cross-link patch can land then) — OQ4 resolution; (b) Task 6 substituted in-memory `re.search` + `yaml.safe_load` for the spec's scratch-file flow (same gate, no scratch-file lifecycle risk). Tasks 1-8 checked; Task 9 deferred-work updates intentionally post-review per spec. 7 OQs captured for code-review (slug availability, github.token default, schemafile URL vs builtin, OQ4 resolution, env-var passthrough, runbook checkbox amendment, comment density). Epic 4 next after this story's code-review: 4-3b-gitlab-ci-catalog-component (backlog)."
project_key: NOKEY
tracking_system: file-system
story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

development_status:
  # Epic 1: Foundation & First Auto-Tag (GitLab + branch-prefix)
  epic-1: done
  1-1-bootstrap-public-scaffolding-from-modern-di: done
  1-2-settings-layer-with-aliaschoices-and-provenance: done
  1-3-errors-runresult-output-redaction: done
  1-4-retryingtransport-with-retry-policy: done
  1-5-gitlabprovider-four-endpoints-via-httpx2: done
  1-6-branchprefixstrategy-100-branch-coverage: done
  1-7-wire-di-groups-and-typer-entrypoint: done
  epic-1-retrospective: done

  # Epic 2: Conventional Commits Strategy & Per-Repo Switching
  epic-2: done
  2-1-conventional-commits-strategy-and-per-repo-switching: done
  epic-2-retrospective: done

  # Epic 3: Pre-flight Diagnostics (semvertag doctor)
  epic-3: done
  3-1-doctor-chain-runner-and-exit-code-dominance: done
  3-2-doctor-typer-subcommand-and-json-form: done
  epic-3-retrospective: done

  # Epic 4: Public-Launch Readiness - Trust Surface, Distribution & Shadow-Mode
  epic-4: in-progress
  4-1-ci-workflow-polish: done
  4-2-publish-workflow-via-trusted-publishing: done
  4-3a-github-actions-marketplace-wrapper: done
  4-3b-gitlab-ci-catalog-component: backlog
  4-4-mkdocs-site-content: backlog
  4-5-three-migration-guides: backlog
  4-6-trust-surface-markdown: backlog
  4-7-readme-hero-and-issue-templates: backlog
  4-8-shadow-mode-parity-validation: backlog
  epic-4-retrospective: optional
