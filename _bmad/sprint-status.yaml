# generated: 2026-05-26
# last_updated: 2026-05-30 (Story 4.3b contextualised — Status: ready-for-dev. 628-line story file at _bmad/4-3b-gitlab-ci-catalog-component.md: 14 dev-facing ACs expanding the 3 epic G/W/Ts, 9 tasks, 15 critical constraints, 15 anti-patterns, 7 OQs. Critical engineering decisions baked in: descriptor path is templates/semvertag.yml (canonical GitLab CI Catalog discovery; deviates from architecture.md:1067-1069 + epics.md:779 which prescribe .gitlab/catalog/component.yml — flagged as OQ1 highest-priority); AC5 schema-validate uses inline Python heredoc + yaml.safe_load_all structural check (no Catalog-component schema in check-jsonschema builtins or schemastore.org as of 2026-05-30); single input `strategy` (token via existing CI_JOB_TOKEN/SEMVERTAG_TOKEN alias chain — no token input surfaced); image: python:3.13-slim + pip install uv (not vendored container, not ghcr.io/astral-sh/uv); $[[ inputs.strategy ]] substitution syntax (NOT GitHub ${{ }}); no branding block (Catalog has no equivalent); cross-host Catalog publish gap (semvertag is GitHub-only; Catalog requires GitLab-hosted) deferred to OQ2/Story 4.7. Files: templates/semvertag.yml NEW, docs/providers/gitlab.md NEW (no preview banner since GitLab provider is fully implemented), mkdocs.yml +1 line, ci.yml +1 step. Do-not-touch list includes 4-3a outputs and the architecture-suggested .gitlab/ path.)
# project: semvertag
# project_key: NOKEY
# tracking_system: file-system
# story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-26
last_updated: 2026-05-30
project: semvertag
last_updated_note: "Story 4.3b contextualised — Status: ready-for-dev. 628-line story file at _bmad/4-3b-gitlab-ci-catalog-component.md. Structure mirrors 4.3a: 14 dev-facing ACs expanding the 3 epic G/W/Ts (epics.md:771-790), 9 tasks (Task 1 author templates/semvertag.yml; Task 2 author docs/providers/gitlab.md; Task 3 mkdocs nav insert; Task 4 ci.yml schema-validate step; Tasks 5-8 gates + records; Task 9 post-review deferred-work), 15 critical constraints, 15 anti-patterns, 7 OQs. Critical engineering decisions baked in: (1) Descriptor path is `templates/semvertag.yml` — the GitLab-canonical Catalog-discoverable path. This DEVIATES from architecture.md:1067-1069 + epics.md:779 which prescribe `.gitlab/catalog/component.yml`; Constraint 1 + OQ1 (HIGHEST PRIORITY) cite Context7-verified GitLab docs proving Catalog ingestion strictly scans `templates/`. (2) AC5 schema-validate uses inline Python heredoc + yaml.safe_load_all + structural sanity checks — `check-jsonschema` has NO `vendor.gitlab-ci-component` builtin (verified 2026-05-30 against full builtin list) and schemastore.org publishes no Catalog component schema. (3) Single input `strategy` (typed `string`, options `[branch-prefix, conventional-commits]`, default `branch-prefix`); NO `token` input (the settings-layer's CI_JOB_TOKEN/SEMVERTAG_TOKEN alias chain handles it natively); NO `gitlab_endpoint`/`project_id` inputs (env-driven). (4) `image: python:3.13-slim` + `pip install --quiet --no-cache-dir uv` (not vendored container, not `ghcr.io/astral-sh/uv`). (5) `$[[ inputs.strategy ]]` substitution syntax (GitLab Catalog v17.0+, NOT GitHub Actions `${{ }}`). (6) NO `branding:` block (Catalog has no equivalent — UI presentation comes from repo description + README). (7) Cross-host gap: semvertag is GitHub-canonical but Catalog requires GitLab-hosted; AC9 explicitly DEFERS the publish mechanism to OQ2/Story 4.7 (3 options: mirror+sync, manual push per release, parallel GitLab-only repo). Files: templates/semvertag.yml NEW (~25 LOC), docs/providers/gitlab.md NEW (~120 LOC, 6 H2 sections mirroring github.md, NO preview banner since GitLab provider is fully implemented), mkdocs.yml +1 line (GitLab CI leaf alphabetically before GitHub Actions), ci.yml +1 step. Do-not-touch list includes 4-3a outputs (action.yml, docs/providers/github.md) and the architecture-suggested .gitlab/ path. PRD anchors: FR40 (Catalog adoption), FR42 (uvx zero-install), FR45 (discoverability), Journey 1 (Petr the GitLab SRE). Constraint 15 corrects PRD's GitLab 15.0+ floor to 17.0+ (Catalog v1 requirement). Anti-pattern 1 covers the `${{ }}` vs `$[[ ]]` syntax footgun. Sprint plan next after this story's dev: 4-3a was sequenced first by sprint choice (independent siblings)."
project_key: NOKEY
tracking_system: file-system
story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

development_status:
  # Epic 1: Foundation & First Auto-Tag (GitLab + branch-prefix)
  epic-1: done
  1-1-bootstrap-public-scaffolding-from-modern-di: done
  1-2-settings-layer-with-aliaschoices-and-provenance: done
  1-3-errors-runresult-output-redaction: done
  1-4-retryingtransport-with-retry-policy: done
  1-5-gitlabprovider-four-endpoints-via-httpx2: done
  1-6-branchprefixstrategy-100-branch-coverage: done
  1-7-wire-di-groups-and-typer-entrypoint: done
  epic-1-retrospective: done

  # Epic 2: Conventional Commits Strategy & Per-Repo Switching
  epic-2: done
  2-1-conventional-commits-strategy-and-per-repo-switching: done
  epic-2-retrospective: done

  # Epic 3: Pre-flight Diagnostics (semvertag doctor)
  epic-3: done
  3-1-doctor-chain-runner-and-exit-code-dominance: done
  3-2-doctor-typer-subcommand-and-json-form: done
  epic-3-retrospective: done

  # Epic 4: Public-Launch Readiness - Trust Surface, Distribution & Shadow-Mode
  epic-4: in-progress
  4-1-ci-workflow-polish: done
  4-2-publish-workflow-via-trusted-publishing: done
  4-3a-github-actions-marketplace-wrapper: done
  4-3b-gitlab-ci-catalog-component: ready-for-dev
  4-4-mkdocs-site-content: backlog
  4-5-three-migration-guides: backlog
  4-6-trust-surface-markdown: backlog
  4-7-readme-hero-and-issue-templates: backlog
  4-8-shadow-mode-parity-validation: backlog
  epic-4-retrospective: optional
