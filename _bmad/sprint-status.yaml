# generated: 2026-05-26
# last_updated: 2026-05-30 (Story 4.2 done — code-review applied + 4 pre-merge action items closed; actions/upload-artifact pinned @ea165f8d…v4; uv version bumped to 0.11.17; all gates green)
# project: semvertag
# project_key: NOKEY
# tracking_system: file-system
# story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-26
last_updated: 2026-05-30
project: semvertag
last_updated_note: "Story 4.2 code-review applied — Status: review → in-progress. 6 decision-needed resolved (4 → patches, 2 → dismissed accepted-risk). 14 patches landed: (a) concurrency group includes event_name (release+dispatch no longer collide); (b) actions/checkout `with.ref` so workflow_dispatch checks out the dispatched tag, not main HEAD; (c) `setup-uv with.version: \"0.5.31\"` (TODO: verify against current stable); (d) job-level `env.UV_INDEX_URL: https://pypi.org/simple/`; (e) new `Verify release commit is on main` guard step on release events; (f) tightened SemVer regex per §9/§10 (rejects `1.0.0-01` / `1.0.0-.alpha`); (g) `if: success()` on Build/Upload/Publish steps; (h) `uv build --out-dir dist` + `uv publish dist/*`; (i) new `actions/upload-artifact@v4` step (TODO: SHA-pin before merge, add SHA to AC7 grep canary); (j) runbook AC8 header swap to spec-literal `## Cutting a release (≤5 minutes, target NFR13 + 5-min-from-merge-to-PyPI)`; (k) runbook adds `> Note:` to replace `<org>` with real owner before first release; (l) runbook re-frames `1.0.0-rc.1` / `1.0.0+build.123` as guard-pass-but-PyPI-reject; (m) runbook adds partial-upload PyPI 400 troubleshooting bullet; (n) runbook adds post-release `[project.version]` policy + required-reviewer SLO caveat + CHANGELOG.md→GitHub-release-body until Story 4.6. 5 items deferred to `_bmad/deferred-work.md §4-2`. Gates green post-patch: `yaml.safe_load` clean, `just lint-ci` clean, `mkdocs build --strict` clean (0.20s). Strict-SemVer regex smoke (17 cases) all PASS. Status: in-progress until pre-merge action items land (SHA-pin upload-artifact, verify uv version, re-run AC7 canary). 2 OQs (OQ11 regex, OQ12 dispatch tag) settled by patches (g) and (b). All 4 pre-merge action items closed 2026-05-30: actions/upload-artifact pinned to ea165f8d65b6e75b540449e92b4886f43607fa02 (v4); setup-uv version bumped from placeholder 0.5.31 to current stable 0.11.17; AC7 credential canary re-run clean; gates re-run clean (yaml.safe_load, just lint-ci, mkdocs build --strict 0.20s, just test 425/425 in 1.51s). Status: review → in-progress → done. Epic 4 next: 4-3a-github-actions-marketplace-wrapper (backlog)."
project_key: NOKEY
tracking_system: file-system
story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

development_status:
  # Epic 1: Foundation & First Auto-Tag (GitLab + branch-prefix)
  epic-1: done
  1-1-bootstrap-public-scaffolding-from-modern-di: done
  1-2-settings-layer-with-aliaschoices-and-provenance: done
  1-3-errors-runresult-output-redaction: done
  1-4-retryingtransport-with-retry-policy: done
  1-5-gitlabprovider-four-endpoints-via-httpx2: done
  1-6-branchprefixstrategy-100-branch-coverage: done
  1-7-wire-di-groups-and-typer-entrypoint: done
  epic-1-retrospective: done

  # Epic 2: Conventional Commits Strategy & Per-Repo Switching
  epic-2: done
  2-1-conventional-commits-strategy-and-per-repo-switching: done
  epic-2-retrospective: done

  # Epic 3: Pre-flight Diagnostics (semvertag doctor)
  epic-3: done
  3-1-doctor-chain-runner-and-exit-code-dominance: done
  3-2-doctor-typer-subcommand-and-json-form: done
  epic-3-retrospective: done

  # Epic 4: Public-Launch Readiness - Trust Surface, Distribution & Shadow-Mode
  epic-4: in-progress
  4-1-ci-workflow-polish: done
  4-2-publish-workflow-via-trusted-publishing: done
  4-3a-github-actions-marketplace-wrapper: backlog
  4-3b-gitlab-ci-catalog-component: backlog
  4-4-mkdocs-site-content: backlog
  4-5-three-migration-guides: backlog
  4-6-trust-surface-markdown: backlog
  4-7-readme-hero-and-issue-templates: backlog
  4-8-shadow-mode-parity-validation: backlog
  epic-4-retrospective: optional
