# generated: 2026-05-26
# last_updated: 2026-05-29 (Story 4.1 code-review complete — Status: done; 10 patches applied across ci.yml + dependency-update.yml)
# project: semvertag
# project_key: NOKEY
# tracking_system: file-system
# story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-26
last_updated: 2026-05-29
project: semvertag
last_updated_note: "Story 4.1 code-review complete — Status: done. 10 patches applied (5 from decision-needed + 5 direct): (1) gh workflow run ci.yml --ref re-trigger step added so the quarterly dependency-update PR gets CI signal (closes NFR26 gap the impl otherwise had); (2) uv export --format requirements-txt --frozen --no-hashes step prepended to pip-audit so the audit scans the actually-shipped uv.lock pins, not pyproject's unbounded specifiers (closes NFR12 gap); (3) SHA-pinned all 4 actions in dependency-update.yml (write-scope workflow): actions/checkout@34e1148, extractions/setup-just@dd310ad, astral-sh/setup-uv@caf0cab, peter-evans/create-pull-request@c5a7806 (ci.yml stays on tag pins per architecture policy); (4) codecov fork-safe guard tightened to explicit allowlist event_name == 'push' || (pull_request && head.repo.full_name == github.repository); (5) LOC awk tightened to skip docstring bodies — count 1541 → 1535 (still 35 over the 1500 soft target; OQ7 stays); (6) id: create_pr + pull-request-number step-summary capture (AC9); (7) unreachable uv sync --frozen dropped from daily safety-check, kept only uv lock --check wrapped warning-only; (8) issues: write + actions: write permissions added for label auto-create and gh workflow run re-trigger; (9) add-paths: uv.lock added to peter-evans to scope staging; (10) MODE routing rewritten as explicit Route step that fails loud on unexpected event/schedule. Gates green: lint-ci, 425 tests, branch/cc/doctor 100% branch, ty, uv build, both workflow YAMLs parse via yaml.safe_load. AC11 byte-exact preserved (git diff HEAD -- semvertag/ tests/ pyproject.toml Justfile empty). 8 deferred items captured in deferred-work.md. Epic 4 next: 4-2-publish-workflow-via-trusted-publishing (backlog)."
project_key: NOKEY
tracking_system: file-system
story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

development_status:
  # Epic 1: Foundation & First Auto-Tag (GitLab + branch-prefix)
  epic-1: done
  1-1-bootstrap-public-scaffolding-from-modern-di: done
  1-2-settings-layer-with-aliaschoices-and-provenance: done
  1-3-errors-runresult-output-redaction: done
  1-4-retryingtransport-with-retry-policy: done
  1-5-gitlabprovider-four-endpoints-via-httpx2: done
  1-6-branchprefixstrategy-100-branch-coverage: done
  1-7-wire-di-groups-and-typer-entrypoint: done
  epic-1-retrospective: done

  # Epic 2: Conventional Commits Strategy & Per-Repo Switching
  epic-2: done
  2-1-conventional-commits-strategy-and-per-repo-switching: done
  epic-2-retrospective: done

  # Epic 3: Pre-flight Diagnostics (semvertag doctor)
  epic-3: done
  3-1-doctor-chain-runner-and-exit-code-dominance: done
  3-2-doctor-typer-subcommand-and-json-form: done
  epic-3-retrospective: done

  # Epic 4: Public-Launch Readiness - Trust Surface, Distribution & Shadow-Mode
  epic-4: in-progress
  4-1-ci-workflow-polish: done
  4-2-publish-workflow-via-trusted-publishing: backlog
  4-3a-github-actions-marketplace-wrapper: backlog
  4-3b-gitlab-ci-catalog-component: backlog
  4-4-mkdocs-site-content: backlog
  4-5-three-migration-guides: backlog
  4-6-trust-surface-markdown: backlog
  4-7-readme-hero-and-issue-templates: backlog
  4-8-shadow-mode-parity-validation: backlog
  epic-4-retrospective: optional
