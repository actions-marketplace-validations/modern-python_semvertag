# generated: 2026-05-26
# last_updated: 2026-05-30 (Story 4.3a contexted — backlog → ready-for-dev; comprehensive spec with 14 dev ACs, 9 tasks, 15 constraints + 15 anti-patterns; composite action.yml + docs/providers/github.md + ci.yml schema-validate via uvx check-jsonschema)
# project: semvertag
# project_key: NOKEY
# tracking_system: file-system
# story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-26
last_updated: 2026-05-30
project: semvertag
last_updated_note: "Story 4.3a (github-actions-marketplace-wrapper) contexted: backlog → ready-for-dev. Spec covers Marketplace `action.yml` (composite; `name: 'semvertag'`, `branding: { icon: tag, color: blue }`, inputs `strategy` + `token` (default `${{ github.token }}`), `runs.using: composite` with setup-uv + `uvx semvertag` shelling); `docs/providers/github.md` (six sections incl. byte-exact Journey 3 7-line snippet); `mkdocs.yml` nav gains `Providers → GitHub Actions` (open for 4-3b sibling to extend); `ci.yml` `lint` job gains ONE schema-validation step (`uvx --from check-jsonschema check-jsonschema --schemafile https://json.schemastore.org/github-action.json action.yml` — no new third-party action, no Go binary, preserves Story 4.1/4.2 actionlint-deferral). 14 dev-facing ACs expanded from epic's 3 narratives; 9 task groups including a manual snippet smoke check (Task 6); 15 numbered architectural constraints (composite-step `shell:` mandatory + non-inheriting, `secrets.GITHUB_TOKEN` NOT available inside composite → use `github.token`, no `actions/checkout` inside composite, no uv-version pin (read-scope context), `name: 'semvertag'` Marketplace-globally unique, `<org>` placeholder preserved for Story 4.7); 15 anti-patterns including missing `shell:`, JS-action/Docker-action key leakage, `secrets.GITHUB_TOKEN` inside composite. 7 OQs captured (slug availability, env vs github.token default, schemafile vs builtin-schema, dead links to docs/strategies until Story 4.4, explicit env-var passthrough, runbook addition for one-time `Publish this Action to the GitHub Marketplace` checkbox, YAML comment density). Touches: action.yml NEW; docs/providers/github.md NEW; docs/providers/ NEW dir; mkdocs.yml UPDATE (4 LOC); ci.yml UPDATE (~5 LOC); sprint-status.yaml UPDATE; this story file UPDATE; deferred-work.md UPDATE post-review only. Do-not-touch: semvertag/, tests/, pyproject.toml, Justfile, README.md, LICENSE, docs/index.md, docs/contributing/, publish.yml, dependency-update.yml, .gitlab/ (Story 4.3b sibling). Epic 4 ongoing: 4-2 done; 4-3a ready-for-dev; 4-3b/4-4/4-5/4-6/4-7/4-8 backlog."
project_key: NOKEY
tracking_system: file-system
story_location: /Users/kevinsmith/src/pypi/autosemver/_bmad

development_status:
  # Epic 1: Foundation & First Auto-Tag (GitLab + branch-prefix)
  epic-1: done
  1-1-bootstrap-public-scaffolding-from-modern-di: done
  1-2-settings-layer-with-aliaschoices-and-provenance: done
  1-3-errors-runresult-output-redaction: done
  1-4-retryingtransport-with-retry-policy: done
  1-5-gitlabprovider-four-endpoints-via-httpx2: done
  1-6-branchprefixstrategy-100-branch-coverage: done
  1-7-wire-di-groups-and-typer-entrypoint: done
  epic-1-retrospective: done

  # Epic 2: Conventional Commits Strategy & Per-Repo Switching
  epic-2: done
  2-1-conventional-commits-strategy-and-per-repo-switching: done
  epic-2-retrospective: done

  # Epic 3: Pre-flight Diagnostics (semvertag doctor)
  epic-3: done
  3-1-doctor-chain-runner-and-exit-code-dominance: done
  3-2-doctor-typer-subcommand-and-json-form: done
  epic-3-retrospective: done

  # Epic 4: Public-Launch Readiness - Trust Surface, Distribution & Shadow-Mode
  epic-4: in-progress
  4-1-ci-workflow-polish: done
  4-2-publish-workflow-via-trusted-publishing: done
  4-3a-github-actions-marketplace-wrapper: ready-for-dev
  4-3b-gitlab-ci-catalog-component: backlog
  4-4-mkdocs-site-content: backlog
  4-5-three-migration-guides: backlog
  4-6-trust-surface-markdown: backlog
  4-7-readme-hero-and-issue-templates: backlog
  4-8-shadow-mode-parity-validation: backlog
  epic-4-retrospective: optional
