# Deferred Work

Tracking issues raised during code review that were intentionally not fixed in-cycle. Each entry has a one-line reason for deferral so future reviews and planning can decide when to act.

## Deferred from: code review of 1-1-bootstrap-public-scaffolding-from-modern-di (2026-05-26)

- `semvertag` console script references missing `semvertag.__main__:main` [pyproject.toml:34] — Story 1.7 lands the typer entrypoint; bootstrap intentionally ships package skeleton only.
- `just install` mixes `uv lock --upgrade` with `uv sync --frozen` — every install re-locks, hurting reproducibility for new contributors. Template-inherited from modern-di; revisit if/when semvertag diverges from template's install policy.
- Duplicate `--cov` flags between `addopts = "--cov=. --cov-report term-missing"` and CI's `just test . --cov=. --cov-report xml`. Reports merge into both `term-missing` and `xml` outputs; positional `.` also overrides `testpaths = ["tests"]`. Template-inherited.
- `uv_build` build-backend unpinned (`requires = ["uv_build"]`) — future major release can break reproducible builds. Template-inherited.
- `eof-fixer .` walks repo root (`.venv/`, `_autosemver_reference/`, `_bmad/`) [Justfile:9, Justfile:20] — relies on the tool's default exclude behavior. Template-inherited; `auto-typing-final` scoping is a separate decision in the story.
- `<org>` URL placeholders in `pyproject.toml [project.urls]` and `mkdocs.yml repo_url`/`extra.social` — spec-acknowledged; pre-launch resolution per Launch Decisions Pending in prd.md.
- `codecov-action@v4.0.1` pinned to an early-v4 patch with known token-handling bugs. Template-inherited; consider bumping when next touching CI.
- No fork-safe guard on codecov upload — fork PRs lack `CODECOV_TOKEN` and the action fails noisily on every external contribution. Template-inherited.
- No `timeout-minutes` on CI jobs — runaway jobs default to GitHub's 360-minute limit. Template-inherited.
- No explicit `permissions:` block on the workflow — defaults to repo-configured `GITHUB_TOKEN` permissions; broader-than-needed blast radius. Template-inherited.
- `setup-uv` `cache-dependency-glob: "**/pyproject.toml"` misaligned with `uv lock --upgrade` running every install — cache key doesn't change on lock-only updates. Template-inherited; moot until install/lock policy changes.
- `.gitignore` carries `plan.md` entry inherited from modern-di — leaks template author's personal workflow into every downstream project. Template-inherited.
- `.gitignore` uses `__pycache__/*` rather than `__pycache__/` — ignores contents but leaves empty `__pycache__` dirs stageable. Template-inherited.
- Production dependencies (`typer`, `rich`, `semver`, `pydantic-settings`, `modern-di-typer`, `httpx2`) carry no version pins or lower bounds. NFR12 commit-lockfile policy partially mitigates installation drift but does not protect against breaking upstream releases when consumers install fresh.
- `[tool.uv.build-backend]` declares no sdist `source-exclude` — `_autosemver_reference/`, `_bmad/`, `docs/`, top-level dotfiles may all ship inside the sdist when `uv build` runs. Revisit at first PyPI publish (Story 4.2).
- Concurrency `group: ${{ github.head_ref || github.run_id }}` falls back to unique `run_id` on push events — concurrent main-branch runs are never cancelled. Template-inherited.
- `actions/checkout@v4` default `fetch-depth: 1` and no `fetch-tags: true` — fine while only the smoke test exists, but a semver-tagging tool will need git history + tags once provider tests land (Story 1.5+).
- `uv sync --all-extras` is a no-op (no `[project.optional-dependencies]` declared) [Justfile:5]. Template-inherited; harmless today but confusing for new contributors.

## Deferred from: code review of 1-3-errors-runresult-output-redaction (2026-05-27)

- Token-family coverage gaps in `_redact.py:6-11` — `gho_`, `ghu_`, `ghs_`, `ghr_`, `github_pat_`, AWS `AKIA`/`ASIA`, OpenAI `sk-*`, Slack `xox*`, Stripe `sk_live_*`, Azure SAS `sig=`, Bitbucket `ATCTT…`. AC7 explicitly scoped this story to four families; Task 3.3 flagged the rest for Story 1.5/3.x.
- Full git SHAs (40+ hex chars) inside error/progress messages are redacted to `***` [`_redact.py:10`] — accepted architectural trade-off; revisit when token-family expansion happens.
- `BrokenPipeError` / `OSError` on `sys.stdout.write` and `Console.print` [`_output.py:30, 33, 36, 48-50, 53`] — `semvertag ... | head` will traceback today. Belongs to Story 1.7 CLI top-level handler.
- `build_rich_output` / `build_json_output` have no `force_terminal` / `color_system` override [`_output.py:66-78`] — Story 1.7 wires CLI flags; revisit when `--no-color` / `--color=always` semantics are decided.
- `JsonOutput.emit` doesn't pass the serialized payload through `redact()` [`_output.py:47-50`] — if `RunResult.reason` ever carries a token (e.g. provider error text), it leaks unredacted in JSON output. Decide in Story 1.5/1.7 when reason values are populated.
- Long Rich messages wrap at default `width=80` and may break single-line log expectations [`_output.py:30, 33`] — redaction is applied pre-wrap so security is unaffected; add `soft_wrap=True`/`no_wrap=True` only if downstream log parsers complain.
- Marginal redact-test coverage gaps: `redact("")`, multi-line input, two adjacent tokens, uppercase-only hex, hex bordered by `-`/`_`/`.`/`:` [`tests/unit/test_redact.py`] — beyond AC8 text; 100% line coverage already met.
- AC9 narrative example uses 19-char token body (`"glpat-RealToken1234567890"`) while pattern requires ≥20 [spec `1-3-...md` AC9 narrative] — cosmetic spec fix; tests use a 20+-char fixture.
- Dev Agent Record §Debug Log References doesn't mention the extra token families Task 3.3 asked the dev to note for Story 1.5/3.x — recorded here so the next refactor sees the list.

## Deferred from: code review of 1-4-retryingtransport-with-retry-policy (2026-05-27)

- Response objects from failed retry attempts are discarded without `response.close()` [`semvertag/_transport.py:33-39`] — harmless under `MockTransport`, but real `httpx2.HTTPTransport` may leak a connection back to the pool on each discarded 5xx. Architecture sketch (story line 309) also discards. Verify in Story 1.5 GitLabProvider integration tests; add `response.close()` to the retry loop if pool drainage shows leakage.
- `RETRYABLE_EXCEPTIONS` omits `httpx2.ConnectTimeout` and `httpx2.PoolTimeout` [`semvertag/_transport.py:10-15`] — AC1 fixes the tuple to these exact 4 types. `ConnectTimeout` is the most glaring omission; production retry layers commonly include it. Changing the tuple requires an AC1 revision in a future story.
- Retries apply to non-idempotent methods (POST/PATCH) unconditionally — `handle_request` does not inspect `request.method` [`semvertag/_transport.py:27-49`]. semvertag's only POST is tag creation; the GitLabProvider (Story 1.5) translates 409 "tag already exists" so duplicate-tag risk is mitigated downstream. Revisit if a non-idempotent endpoint without server-side idempotency ever lands.

## Deferred from: code review of 1-5-gitlabprovider-four-endpoints-via-httpx2 (2026-05-27)

- `_translate_status` hardcodes "Retries exhausted after 3 attempts" in the 429 / 5xx cause messages [`semvertag/providers/gitlab.py:336, 339`] — spec AC7 prescribes the exact strings, so the implementation is correct, but the text silently drifts if `_transport.MAX_ATTEMPTS` ever changes. Revisit if/when the retry policy is tunable.
- `_TAG_EXISTS_FRAGMENT = "already exists"` is English-only [`semvertag/providers/gitlab.py:78, 168`] — A GitLab server with `Accept-Language: fr` returns "le tag existe déjà" and the substring match fails; duplicate-tag errors fall into the generic "Request rejected by GitLab: 400" path. Fix requires either an explicit `Accept-Language: en` header in `_auth_headers()` (architectural choice) or matching GitLab's structured-error-code (`{"message": {"tag_name": ["already exists"]}}`) instead of free-text.
- `tests/conftest.py::_default_handler` returns 201 for ANY POST to `/repository/tags` regardless of payload [`tests/conftest.py:398-399`] — payload-shape regressions in `create_tag` would not surface in fixtures that rely on the default handler. Tighten when adding payload-strict default behavior.
- `tests/conftest.py::_default_handler` falls back to 404 for unknown paths, which the provider translates to `ConfigError("project not found")` — a typo in any URL builder in `gitlab.py` would be misdiagnosed as a project-not-found error rather than failing loudly [`tests/conftest.py:400`]. Consider raising `AssertionError` (or `httpx2.Response(599)`) from the fallback so unintended requests fail fast.
- No pagination-loop detection in `list_tags` — a broken proxy returning the same `Link: rel="next"` URL each iteration burns up to 100 calls and accumulates duplicate tags before tripping the cap [`semvertag/providers/gitlab.py:128-147`]. Add a `seen: set[str]` guard if seen in the wild.
- `_MAX_TAG_PAGES * _TAGS_PER_PAGE = 10_000` hard ceiling is unreachable for legitimately large monorepos (Linux kernel mirrors, per-deploy tag projects) [`semvertag/providers/gitlab.py:64-65`] — spec mandates the 100-page cap; revisit if real users hit it.
- `raise ProviderAPIError(...) from exc` chains `httpx2.RequestError`, whose `__str__` typically includes the request URL [`semvertag/providers/gitlab.py:42-43, 68-69, 84-85, 106-107`] — if `SEMVERTAG_GITLAB__ENDPOINT` ever contains userinfo (e.g. `https://oauth2:token@gitlab.example`, an anti-pattern), credentials surface in traceback / logs via `__cause__`. Defense-in-depth: either redact endpoint userinfo at construction or use `raise ... from None` for the request-error path.
- Integration tests reach into `semvertag._transport` to monkey-patch `time.sleep` / `random.uniform` [`tests/integration/test_gitlab_provider.py:848-849, 862-863, 876-877`] — tight coupling to internals. Any refactor of `_transport` (e.g., switching to `secrets.SystemRandom` or moving the sleep call) silently breaks the no-op patching and tests start really sleeping. Would require exposing a `sleep_fn` / `jitter_fn` seam on `RetryingTransport`.

## Deferred from: story 1-6-branchprefixstrategy-100-branch-coverage (2026-05-28)

- CI workflow does not yet enforce 100% branch coverage on `semvertag/strategies/branch_prefix.py`. `Justfile::test-branch-strategies` is the developer-local gate; the equivalent CI job step is Story 4.1 (CI workflow polish) scope.

## Deferred from: story 1-7-wire-di-groups-and-typer-entrypoint (2026-05-28)

- `--default-branch` flag is wired into `settings.default_branch` and recorded in `_provenance`, but `GitLabProvider.get_default_branch()` (`semvertag/providers/gitlab.py:47-66`) does NOT yet consume it — it always asks the API. Per AC6.6 this is intentional v1.0 scope; activating the override requires either extending the `Provider` protocol with `get_default_branch(override: str | None = None)` or accepting `settings.default_branch` at provider construction. Both are protocol-surface changes that warrant their own story.
- `@modern_di_typer.inject` + `FromDI(...)` was dropped from `__main__.py` because the framework reads its container from `app.info.context_settings["obj"]["di_container"]` set at `setup_di(...)` time, but Story 1.7 needs to build the container per-invocation (after CLI overlay). The current entrypoint resolves `UseCasesGroup.semvertag_use_case` via `container.resolve_provider(...)` inside the callback body — equivalent functionality, simpler control flow. If a future story benefits from the `@inject` integration (e.g., to inject use cases into multiple subcommands), revisit by either pre-resolving Settings in `setup_di`-time scope or by refactoring the container build to a one-shot static binding.
- The use case constructs `SemvertagUseCase` via DI with `output=OutputsGroup.rich_output`, then `__main__.py` rebuilds a swapped-output instance via `_run_with_output_override` to honor the entrypoint-built output (which has the right `quiet`/`json` flags). The container-side override of `rich_output → json_output` (via `container.override`) only swaps the Output used by the DI-resolved use case, but `__main__.py` needs the output object BEFORE the try-block (for `output.error(...)` in the except clause), so the override-then-swap pattern is the simplest reconciliation. Cleaner alternative: pass the entrypoint-constructed output instance into the container as `context={Output: output_instance}` and have `UseCasesGroup` consume it via a `ContextProvider`. Defer to a future refactor.
- Newer Click (>= 8.2) removed `CliRunner(mix_stderr=False)` — stdout/stderr are now separated by default. Integration-test fixtures use no-arg `CliRunner()` accordingly. If we ever pin to an older Click version, fixtures need `mix_stderr=False` back.
- All Factories in `ioc.py` declare `skip_creator_parsing=True` with explicit `kwargs={...}` to silence `UserWarning: Failed to resolve type hints` triggered by the architecture-mandated lazy provider/strategy imports. Trade-off: modern-di no longer auto-wires dependencies by type — every cross-Group dependency must be spelled out in `kwargs`. Document as the project convention for future Groups.
- `httpx2.Client` lifecycle: `CacheSettings(finalizer=_close_provider_client)` on `ProvidersGroup.gitlab_provider` closes the client when the container exits its `with` block. The finalizer does NOT fire when the provider is overridden via `Container.override(...)` (overrides bypass the factory cache). Tests use `MockTransport` which has no IO to close, so this is benign — but if a future test scenario uses a real `httpx2.HTTPTransport` via the override path, it must close the client manually.

## Deferred from: code review of 1-6-branchprefixstrategy-100-branch-coverage (2026-05-28)

- `BranchPrefixStrategy.decide` misclassifies back-merges into a feature branch as `MINOR` [`semvertag/strategies/branch_prefix.py:23-26`] — `"Merge branch 'main' into feature/x"` matches `"feature/"` via substring even though `feature/x` is the target, not the source. AC4 mandates the substring approach as intentional; fixing this requires a spec revision (parse out the quoted source-branch only, or restrict the match to the canonical first-line `Merge branch '<src>'` form).
- `BranchPrefixStrategy.decide` re-bumps `MINOR` for `Revert "Merge branch 'feature/x' ..."` [`semvertag/strategies/branch_prefix.py:23-26`] — a revert of a feature merge currently produces another `MINOR` bump instead of `NONE` or a rollback. Requires explicit `Revert "Merge…"` / `Reapply "Merge…"` prefix handling in a future spec revision.
- ~~`BranchPrefixConfig` has no `Field(min_length=1)` on `minor` / `patch` tuples or non-empty validation on `merge_mark_text`~~ — **RESOLVED 2026-05-28** by post-review patch: added `Field(min_length=1)` and `Annotated[str, Field(min_length=1)]` to all three fields; 6 new tests cover the rejection paths.
- ~~Multi-line merge commit bodies can match prefixes anywhere in the message~~ — **RESOLVED 2026-05-28** by post-review patch: `decide()` now restricts matching to the first line via `commit.message.split("\n", 1)[0]`; 3 new tests pin the behavior.
- Default `merge_mark_text="Merge branch"` misses common real-world merge formats: GitHub squash (`"Add feature (#123)"`), GitHub merge-PR (`"Merge pull request #N from user/feature/x"`), `"Merge remote-tracking branch 'origin/feature/x'"`, and octopus merges (`"Merge branches 'feature/a' and 'bugfix/b'"`) — all classified `NONE` with defaults. Users on those workflows must override per-repo; consider shipping documented presets / a small recipe table in the v1.0 docs.
- Test fixture matrix omits prefix case-sensitivity, whitespace-only, non-ASCII, and very-long-message scenarios [`tests/unit/test_branch_prefix_strategy.py:18-69`] — 100% line + branch coverage is achieved without exercising `"Feature/x"`, `"   "`, `"合并分支 feature/中文"`, or `"A" * 10000`. Pin behavior when case-sensitivity is explicitly decided.
- ~~No test for empty-tuple configs~~ — **RESOLVED 2026-05-28** alongside the validation patch above; covered by `test_raises_validation_error_when_config_field_is_empty` parametrized fixture.
- `uv run --no-sync` in `test-branch-strategies` recipe may execute against a stale environment [`Justfile:26`] — template-inherited; applies to existing `test` / `test-branch` recipes too. Revisit when install/lock policy changes.
- `pytest -o "addopts="` in `test-branch-strategies` doesn't override `PYTEST_ADDOPTS` env var [`Justfile:26`] — irrelevant locally, but Story 4.1 (CI workflow polish) should `unset PYTEST_ADDOPTS` (or set it empty) when invoking the 100% gate to prevent CI-environment skew.

## Deferred from: code review of 1-6-branchprefixstrategy-100-branch-coverage second pass (2026-05-28)

(After applying two post-review patches — `Field(min_length=1)` validation + first-line-only matching — these new edge cases surfaced. None block v1.0; all are edge-of-edge-case cleanup.)

- `_NonEmptyStr` (`Field(min_length=1)`) accepts whitespace-only / control-char strings [`semvertag/strategies/branch_prefix.py:9`] — `BranchPrefixConfig(minor=(" ",))`, `merge_mark_text="\t"`, or `merge_mark_text="\n"` all pass validation but make the strategy misbehave. Tightening requires `pattern=r"\S"` plus a per-field validator. Defer to a dedicated config-validation hardening pass.
- CRLF line endings leave trailing `\r` in `subject` [`semvertag/strategies/branch_prefix.py:26`] — `commit.message.split("\n", 1)[0]` keeps the `\r` for Windows-origin messages. Use `splitlines()[0]` (with empty guard) to normalise.
- Unicode line separators (U+2028, U+2029) are not split by `split("\n", 1)` [`semvertag/strategies/branch_prefix.py:26`] — exotic but real-world risk on internationalised tools. Same fix as above via `splitlines()`.
- Message starting with a newline yields empty `subject` and returns `Bump.NONE` even when line 2 looks like a real merge [`semvertag/strategies/branch_prefix.py:26`] — use `next((line for line in commit.message.splitlines() if line), "")` to skip leading blanks.
- CLI overlay surfaces raw `pydantic.ValidationError` when `--branch-prefix.minor=()` or similar is passed [`semvertag/_settings.py:225`] — the new validators raise mid-overlay in `_revalidate_nested`, producing a Python traceback rather than a user-friendly CLI error. Catch and wrap in `apply_cli_overlay` (Story 1.7's CLI-ergonomics scope).
- `auto-typing-final` interaction with `typing.TypeAlias` annotation [`semvertag/strategies/branch_prefix.py:9`] — if the tool rewrites `_NonEmptyStr: typing.TypeAlias = ...` to `typing.Final`, pydantic stops recognising the annotation and validators silently misfire. `lint-ci` currently passes so this is latent; verify the tool's behaviour before the next refactor pass.
- `typing.TypeAlias` deprecated in Python 3.12+ in favour of the `type` keyword [`semvertag/strategies/branch_prefix.py:9`] — project targets py310 so `UP040` doesn't flag it today. When the target version moves to 3.12+, switch to `type _NonEmptyStr = typing.Annotated[str, pydantic.Field(min_length=1)]`.
- `merge_mark_text` or prefix containing `\n` silently never matches subject-line scoped check [`semvertag/strategies/branch_prefix.py:27-32`] — validation accepts it (`min_length=1` satisfied) but subject-line scoping makes such strings unreachable. Add `pattern=r"^[^\n]+$"` to `_NonEmptyStr` or a `field_validator` that rejects newline-containing inputs.

## Deferred from: code review of 1-7-wire-di-groups-and-typer-entrypoint (2026-05-28)

- `JsonOutput.emit` skips `redact()` on the serialized payload [`semvertag/_output.py:47-50`] — pre-existing from Story 1.3 (`_output.py` is do-not-touch in Story 1.7). Already noted in 1-3 deferred-work; recurrence flagged by Edge Case Hunter against the integration-test surface.
- `_redact.py` token-body length floor of 20 chars leaks short malformed tokens [`semvertag/_redact.py:6-11`] — `--token glpat-shortbody` (≤19 char body) bypasses redaction if surfaced in a `pydantic.ValidationError`. Pre-existing from Story 1.3 (do-not-touch).
- `rich.console.Console(stderr=True)` has no `force_terminal=False` [`semvertag/_output.py:69, 76`] — under `FORCE_COLOR=1` or a `TERM` that Rich considers a real terminal, integration-test stderr substring checks could fail on developer machines. Pre-existing from Story 1.3 (do-not-touch).
- `_pick_latest_semver_tag` behavior on prerelease / build-metadata tags is implementation-defined [`semvertag/_use_case.py:94-99`] — `semver.Version.parse("1.5.0-rc.1")` parses and sorts after `1.4.2`, then `bump_minor()` strips prerelease metadata. No test pins this. FR8 acceptable for v1.0.
- `_inject_top_level_aliases` treats `CI_PROJECT_ID=""` as unset [`semvertag/_settings.py:97-105, 174-180`] — `_find_aliased_env` filters by `if value:`, so empty-string env vars (common in CI when a variable is templated) fall through to default `None`. Result: `ConfigError("Project id missing")` rather than a more pointed "CI_PROJECT_ID is set but empty" error. Acceptable as-is.
- Missing regression test: CLI value survives revalidation when env conflicts [`semvertag/_settings.py:198-200`] — `apply_cli_overlay` re-runs `model_validate(raw_data)`; current model-validators bail when keys are already present, so CLI wins by accident. No test pins this contract.
- Missing test: `--request-timeout` clamping via CLI overlay [`tests/unit/test_settings.py:84-100`] — `_clamp_request_timeout` re-runs during `apply_cli_overlay`; current coverage tests env-side clamping only.
- Missing test: `--json` formatting of `pydantic.ValidationError` at entrypoint [`semvertag/__main__.py:130-133`] — under `--json`, validation errors should arguably emit a JSON error envelope; currently they go to stderr as plain text via `Console(stderr=True)`. No test pins either behavior.
- `--version` falls back to `"0"` silently on `PackageNotFoundError` [`semvertag/__main__.py:30-36`] — misleading vs an explicit "unknown" or non-zero exit. Pyproject ships `"0"` today anyway; Story 4.2 (PyPI trusted publishing) will land a real version.

## Deferred from: code review of 2-1-conventional-commits-strategy-and-per-repo-switching (2026-05-29)

- Future-strategy dispatch guard [`semvertag/ioc.py:154-156`] — current dispatch is `if settings.strategy == "conventional-commits": override(...)` with no `else: raise`. `Settings.strategy: typing.Literal["branch-prefix", "conventional-commits"]` catches typo'd env/CLI values today, but adding a third strategy to the Literal without updating dispatch would silently fall through to `branch-prefix`. Defensive `else: msg = f"Strategy {settings.strategy!r} has no dispatch wired."; raise ConfigError(msg)` would future-proof.
- `ConventionalCommitsConfig` cross-validation [`semvertag/strategies/conventional_commits.py:15-19`] — overlap between `minor_types` and `patch_types` (silently resolved by ordering), empty tuples (everything → NONE), and types unreachable by the regex `[a-z]+` (e.g., `"FEAT"`, `"feat-x"`) are all silently accepted. A pydantic `model_validator` could surface unreachable / empty / overlapping configs at startup. Out of Story 2.1 scope.
- `commit.message == None` AttributeError [`semvertag/_commit_parse.py:5`] — `message.splitlines()` raises if `Commit.message` is `None`. Type annotation declares `str`; depends on Provider Protocol contract. Pre-existing behavior on `BranchPrefixStrategy` too.
- BOM (U+FEFF) at start of message defeats `^[a-z]+` anchor [`semvertag/strategies/conventional_commits.py:11`] — commits authored via certain Windows tooling can carry a leading BOM. Currently returns `Bump.NONE`. Either `subject_line()` strips BOM, or document as out-of-scope.
- `_TOKEN_PATTERN` greedy matches lack upper bound [`semvertag/_redact.py:6-15`] — adjacent concatenated tokens (e.g., `"ghp_AAA...ghp_BBB"`) leak the second prefix after redaction. Over-redaction would be safe; bounded patterns (known token lengths: `ghp_` 36, `github_pat_` 82, etc.) would tighten the matches. Pattern shape matches Story 1.3's pre-existing hex branch.
- Whitespace-only "blank" line between subject and footers is treated as a separator [`semvertag/_commit_parse.py:18,22`] — `line.strip()` rather than `line == ""`. CC v1.0.0 says "blank line" (typically `""`); diff is permissive. Pathological body `"feat: x\n   \nBREAKING CHANGE: leaked"` (intentionally indented body line) would false-trigger MAJOR.
- Hex-token `\b` word-boundary behavior pre-existing [`semvertag/_redact.py:15`] — Python `re` treats `_` as a word char, so `_<40hex>` won't match the hex branch. Not introduced by this diff; flagging because the diff extends the same regex. Already noted in earlier `1-7-wire-di-groups-and-typer-entrypoint` deferred-work.
- `BranchPrefixStrategy` regression test for leading-blank-line message after the `subject_line()` refactor [`tests/unit/test_branch_prefix_strategy.py`] — semantics shifted from `split("\n", 1)[0]` (returns leading blank line if any) to `subject_line()` (skips leading blanks). Blocked: `tests/unit/test_branch_prefix_strategy.py` is on the Story 2.1 do-not-touch list. Pick up when next touching that file.

## Deferred from: code review of 3-1-doctor-chain-runner-and-exit-code-dominance (2026-05-29)

- `resolve_exit_code` raises `StopIteration` if `_exit_code_for_failed_check` ever returns a code not in `_DOMINANCE` [`semvertag/doctor/_checks.py:46`] — intentional trade-off documented in Debug Log References to satisfy the 100% branch gate (`return SemvertagError.exit_code` fallback was unreachable). Future contributors adding a new `SemvertagError` subclass with a fresh `exit_code` must extend `_DOMINANCE` in lockstep, or the resolver will crash on the new code.
- Multi-family cause-string routing bypasses `_DOMINANCE` order [`semvertag/doctor/_checks.py:43-49`] — `_exit_code_for_failed_check` walks fragment families in auth → config → provider_api lexical order. A single cause containing both a `_CONFIG_CAUSE_FRAGMENTS` hit and a `_PROVIDER_API_CAUSE_FRAGMENTS` hit returns ConfigError (2) though ProviderAPI (4) would dominate cross-check. Hypothetical given current `GitLabProvider` emits one cause per check; revisit when Story 3.2 integration tests exercise the real provider.
- Empty `cause=""` on a failed `CheckResult` silently maps to `SemvertagError.exit_code` (1) [`semvertag/doctor/_checks.py:43-49`] — `CheckResult.cause: str` contract makes empty cause unreachable from the current provider but no test pins behavior. Reconsider if `cause` type ever loosens to `str | None`.
- Cause-fragment matching is substring + case-sensitive [`semvertag/doctor/_checks.py:43-49`] — `"Token missing"` collides with any unrelated phrase containing those words; lowercase `"token rejected"` falls through to generic. `providers/gitlab.py` is single owner of cause vocabulary so the coupling is intentional, but Story 3.2 integration tests should catch real drift between gitlab.py wording and `_checks.py` fragments.
- `test_doctor_checks.py` is 294 LOC vs spec budget of ~150-200 [`tests/unit/test_doctor_checks.py`] — spec uses `~`, case count (33) is within `~30` tolerance, 100% branch gate is satisfied. Watch the LOC budget on future test additions; consider extracting fixture helpers if it grows past 350.

## Deferred from: code review of 3-2-doctor-typer-subcommand-and-json-form (2026-05-29)

- `_format_setting_value` will raise `AttributeError` on a missing/typo'd dotted-path key [`semvertag/doctor/_render.py:44-50`] — currently safe because `_provenance` keys come from `_scan_model` on the same `Settings`, but a future refactor that aliases or renames a field could leave a stale provenance entry that the walker can't resolve. Add a defensive `try/except AttributeError → continue` or a path-resolution helper.
- `_build_output_for_flags` raises before the `try:` block in `_doctor_command` [`semvertag/__main__.py:_doctor_command`] — same pattern in `_main_callback`. Rich console construction is not known to raise in practice, but if `force_terminal`/IO setup ever throws, the exception propagates without a `typer.Exit` translation. Move the call inside the `try:`.
- Private API coupling on `Settings._provenance` [`semvertag/doctor/_render.py:55`] — `# noqa: SLF001` documents it; spec Dev Note 5 makes this architectural. Promote to a public accessor (`Settings.provenance`) if a second consumer ever lands.
- Explicit `--token ""` produces an empty `SecretStr` and proceeds [`semvertag/__main__.py:_collect_doctor_overrides`] — the `is not None` guard accepts the empty string; downstream `_redact_token` emits `"***"` and the auth check fails with a confusing "Token rejected"-style cause. Add `if token` (truthy) instead of `is not None`, or raise `ConfigError("--token cannot be empty")`.
- `render_doctor_json` `OSError(non-EPIPE)` from `sys.stdout.flush()` re-raises without translation [`semvertag/__main__.py:277-280`] — hypothetical (EIO on /dev/stdout). The doctor outer except mirrors the main callback's pattern; tighten only if observed.
- `tests/integration/conftest.py` `_clean_env_before_each` omits token aliases [`tests/integration/conftest.py:28-45`] — `SEMVERTAG_GITLAB__TOKEN`, `SEMVERTAG_GITHUB__TOKEN`, `GITHUB_TOKEN`, `BITBUCKET_TOKEN` are not cleared, so a developer with these in their shell may see the redaction test (`test_redacts_token_to_last_four_in_both_human_and_json_output`) compute `_EXPECTED_REDACTED_TOKEN` from the test fixture token but render the dev's token in stdout, failing opaquely. Pre-existing conftest gap; not introduced by this story.

## Deferred from: code review of 4-1-ci-workflow-polish (2026-05-29)

- `uv lock --upgrade` runs quarterly without upper-bound guardrails — `pyproject.toml` declares all production deps unbounded (no caret, no `>=`), so the upgrade can silently jump majors. [`pyproject.toml` + `.github/workflows/dependency-update.yml`] — architectural; belongs in a future dependency-bounds story.
- Concurrency group `dependency-update` does not dedupe stale upgrade PRs across quarters — if a prior PR is unmerged when the next quarter fires, both stay open against `uv.lock` [`.github/workflows/dependency-update.yml:13-15`]. Future polish: add a "close prior chore/dependency-update-* PRs" step.
- Daily safety check at 09:00 UTC runs 365×/year and consumes Actions minutes if the repo is private or privately forked [`.github/workflows/dependency-update.yml:6`]. Public-repo assumption; document in the dependency-update.md page when it lands.
- `cancel-in-progress: false` queues `workflow_dispatch` behind the in-flight daily run [`.github/workflows/dependency-update.yml:15`]. Either document or split concurrency by MODE (`group: dependency-update-${{ env.MODE }}`).
- `find semvertag` LOC pipeline does not exclude any other build-artifact dirs beyond `__pycache__` [`.github/workflows/ci.yml:34`]. None today, but if `.cache/`, `.mypy_cache/`, or generated `*.py` ever land under `semvertag/`, they'd be counted. Defensive-only.
- No `defaults: run: shell: bash` declared on either workflow file. ubuntu-latest defaults to bash, but pinning removes a future-runner-image foot-gun. Defensive-only.
- Codecov v5 prefers `with: token:` over `env: CODECOV_TOKEN`; both work today, but the env form is the secondary documented path [`.github/workflows/ci.yml:68-69`]. Minor convention drift.
- `<org>` placeholder in README badge URLs renders as a stripped HTML tag in GitHub's markdown→HTML pipeline (badges link but the URL is malformed) [`README.md:1-2`]. Story 4.7 owns pre-launch placeholder resolution per Story 1.1 deferred-work convention.
- OQ7 stays: repo is at 1535 LOC (semvertag/**/*.py, non-blank non-comment non-docstring SLOC) — 35 over the NFR21 1500 soft target. The gate now matches "non-blank non-comment" semantics; the overrun is real and persistent. Options for a future polish pass: refactor for headroom, accept the warning as new normal, or amend NFR21 to 1600.

## Deferred from: code review of 4-2-publish-workflow-via-trusted-publishing (2026-05-30)

- `actions/checkout` default `fetch-depth: 1` in `publish.yml` — fine while `pyproject.toml` carries a static `[project.version]`. If the project ever moves to VCS-derived versioning (`hatch-vcs`, `setuptools-scm`), the publish workflow will silently produce wheels with sentinel versions. Re-flag if that switch is ever considered.
- `uv build` runs without `SOURCE_DATE_EPOCH` pinning — wheels are not byte-reproducible across re-runs of the same tag, which compounds the partial-upload PyPI rejection problem (PyPI uses content-hash for filename collision). Reproducibility hardening can land in a follow-up.
- `uv.lock` is not consumed during the publish — `uv build` reads dependency specifiers from `pyproject.toml`, so pip-audit's signal applies only to the locked dev environment, not to the published wheel's `Requires-Dist`. Pre-existing architecture decision; worth re-flagging when supply-chain hardening cycles begin.
- mkdocs `--strict` will fail on any future orphan file under `docs/contributing/` that's not listed in `nav:`. Story 4.2 created the `docs/contributing/` directory; future contributing-docs stories need to update `mkdocs.yml` nav in lockstep.
- Publish workflow installs only Python 3.13; CI matrix tests 3.10–3.14. Pure-Python wheel is `py3-none-any` so this is fine today, but adding any native code to `[tool.uv.build-backend]` would tag the wheel 3.13-only and break installs on the lower CI matrix cells.
